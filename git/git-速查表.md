```bash
# 设置你的名字和邮箱（提交记录会显示）
git config --global user.name "你的名字"
git config --global user.email "your@email.com"

# 推荐：让 Git 显示真实中文文件名（而不是 \xxx 编码）
git config --global core.quotepath false

# 推荐：默认分支名设为 main（更现代）
git config --global init.defaultBranch main
```
